import type { Metadata } from "next";
import { Inter, Poppins } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Indrani Majumdar - Software Development Engineer in Test",
  description: "Professional portfolio of Indrani Majumdar, a Software Development Engineer in Test with expertise in testing, automation, and software development.",
  keywords: ["Software Development Engineer in Test", "SDET", "Testing", "Automation", "Portfolio"],
  authors: [{ name: "Indrani Majumdar" }],
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${poppins.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
